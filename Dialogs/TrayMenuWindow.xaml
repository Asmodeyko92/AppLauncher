<Window x:Class="AppLauncher.Dialogs.TrayMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AppLauncher" Width="244" Height="176"
        WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True"
        Background="Transparent" ShowInTaskbar="False" Topmost="True"
        WindowStartupLocation="Manual" Opacity="0"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        Deactivated="Window_Deactivated" PreviewKeyDown="Window_PreviewKeyDown">
    <Window.Resources>
        <Style x:Key="TrayActionButton" TargetType="Button">
            <Setter Property="Height" Value="43"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Foreground" Value="#FFF4F5F8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Surface" Property="Background" Value="#16FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Surface" Property="Opacity" Value="0.62"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Border Background="#01000000" CornerRadius="12">
            <Border.Effect>
                <DropShadowEffect Color="#05070C" BlurRadius="26" ShadowDepth="5" Opacity="0.60"/>
            </Border.Effect>
        </Border>
        <Border Background="#F51A1D25" BorderBrush="#36FFFFFF"
                BorderThickness="1" CornerRadius="12" Padding="5">
            <Border.RenderTransform>
                <TranslateTransform x:Name="MenuTranslate" Y="9"/>
            </Border.RenderTransform>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="48"/>
                    <RowDefinition Height="1"/>
                    <RowDefinition Height="43"/>
                    <RowDefinition Height="43"/>
                </Grid.RowDefinitions>

                <Grid Margin="12,0,10,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="34"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <Border Width="28" Height="28" CornerRadius="8" Background="#FF665CFF">
                        <TextBlock Text="&#xE80A;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                   Foreground="White" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="7,0,0,0">
                        <TextBlock Text="AppLauncher" Foreground="White" FontWeight="SemiBold" FontSize="13"/>
                        <TextBlock Text="Работает в фоне" Foreground="#8FFFFFFF" FontSize="10" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>

                <Border Grid.Row="1" Height="1" Margin="10,0" Background="#20FFFFFF"/>

                <Button Grid.Row="2" Style="{StaticResource TrayActionButton}" Click="Show_Click">
                    <Grid>
                        <TextBlock Text="&#xE8A7;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                   Foreground="#FF8B82FF" FontSize="15" VerticalAlignment="Center"/>
                        <TextBlock Text="Показать AppLauncher" Margin="31,0,0,0" VerticalAlignment="Center"/>
                    </Grid>
                </Button>

                <Button Grid.Row="3" Style="{StaticResource TrayActionButton}" Foreground="#FFFF7184" Click="Exit_Click">
                    <Grid>
                        <TextBlock Text="&#xE7E8;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                   Foreground="#FFFF7184" FontSize="15" VerticalAlignment="Center"/>
                        <TextBlock Text="Выйти" Margin="31,0,0,0" VerticalAlignment="Center"/>
                    </Grid>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>
